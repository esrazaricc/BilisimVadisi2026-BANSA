services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      BANSA_DB_PATH: /app/data/campaigns.db
      POSTGRES_DSN: ${POSTGRES_DSN:-}
    volumes:
      - ./data:/app/data:ro
